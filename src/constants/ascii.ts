export const ASCII_CONFIG = {
  chars: ' .:-=+*#%@',
  width: 140,
  height: 95,
  padding: 10
}

export const ASCII_IMAGE_CONFIG = {
  width: ASCII_CONFIG.width - (ASCII_CONFIG.padding * 2),
  height: ASCII_CONFIG.height - (ASCII_CONFIG.padding * 2)
}
